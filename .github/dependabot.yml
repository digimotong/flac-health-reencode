version: 2
updates:
  # Keep the pinned actions current. Without this the SHA pins become stale
  # security liabilities rather than hardening.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: "ci"
    groups:
      actions:
        patterns:
          - "*"
